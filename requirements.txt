# AG2 (recommended) - Active fork, matches your code better
ag2[openai]>=0.10.0
# Microsoft AutoGen (fallback/deprecated) - Original but less active
# autogen>=0.2.28  # Old API
# autogen-agentchat>=0.7.0  # New API (completely different)

# Backend API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
python-socketio>=5.10.0

# Core dependencies
ai21
exa-py
panel
fpdf
markdown
python-dotenv
